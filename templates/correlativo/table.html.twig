{% for tr in objects %}
    
    <tr {% if tr.estadocorrelativo == 'Anulado' %} style="background-color: #ffcdd2" {% endif %}>
        <td class="details-control">
            <i class="fa fa-plus-square cl-teal" aria-hidden="true" title="Mostrar más" id="{{'cr' ~ tr.id}}"></i>
        </td>
        <td class="d-none">{{ tr.id }}</td>
        <td>{{ tr.numcorrelativo }}</td>
        <td>{{ tr.fechareg | date('Y-m-d') }}</td>
        <td>
            {{ tr.fksolicitante.nombre ~ ' ' ~ tr.fksolicitante.apellido }}
            {% if tr.fksolicitante.estado == 2 %}
                 <b><i>{{' (Inactivo)' }}</i></b>
            {% endif %}
        </td>
        <td>{{ tr.redactor }}</td>
        <td class="d-none">{{ tr.destinatario }}</td>
        <td>{{ tr.referencia }}</td>
        <td class="d-none">{{ tr.fkcorrelativo.nombre }}</td>
        <td class="d-none">{{ tr.fktiponota.nombre }}</td>
        <td class="d-none">{{ tr.ip }}</td>

        {% if tr.url != null and tr.url != 'N/A' %}
            <td><a href="#" data-file="{{tr.url}}" class="urlcrtv"><i class="material-icons">folder</i> Archivo</a></td>
        {% else %}
            <td>{{ tr.url }}</td>
        {% endif %}

        <td class="d-none">{{ tr.antecedente }}</td>
        <td class="d-none">{{ tr.estadocorrelativo }}</td>
        <td class="d-none">{{ tr.item }}</td>

        {% if tr.urleditable != null and tr.urleditable != 'N/A' %}
            <td><a href="#" data-file="{{tr.urleditable}}" class="urledt"><i class="material-icons">folder_open</i> Archivo</a></td>
        {% else %}
            <td>{{ tr.urleditable }}</td>
        {% endif %}

        <td class="d-none">{{ tr.entrega | date('Y-m-d') }}</td>
        <td class="d-none">{{ tr.fkunidad.nombre }}</td>
        
        {% set cmp = 'Completo' ~ tr.fkunidad.nombre %}
        {% set crp = 'Crear' ~ tr.fkunidad.nombre %}

        {% if tr.urlorigen != null and tr.urlorigen != 'N/A' %}
            <td><a href="#" data-file="{{tr.urlorigen}}" class="urlorg"><i class="material-icons">folder_special</i> Archivo</a></td>
        {% else %}
            <td>{{ tr.urlorigen }}</td>
        {% endif %}
    </tr>
{% endfor %}

